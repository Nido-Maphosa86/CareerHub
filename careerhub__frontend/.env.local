# The real CareerHub.Api backend (used by the apply flow and the close action).
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1

# This app's own origin. Server Components need an absolute URL to fetch the
# app's own Route Handlers (e.g. /api/applications/stats).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Auth.js secret — used to sign the session JWT cookie. Generated per machine.
AUTH_SECRET=aTdijcPBsDo70U4NiCxgG1A+8CAxDPDC/CdmThWYOQc=

